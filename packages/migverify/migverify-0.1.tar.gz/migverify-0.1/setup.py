#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['migverify']

package_data = \
{'': ['*']}

install_requires = \
['click>=6.6']

entry_points = \
{'console_scripts': ['run_migverify = migverify.run_migverify:migverify']}

setup(name='migverify',
      version='0.1',
      description='A script to parse and verify migrate-n infiles with loads',
      author='Anders Goncalves da Silva',
      author_email='andersgs@gmail.com',
      url='https://github.com/andersgs/migverify',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
