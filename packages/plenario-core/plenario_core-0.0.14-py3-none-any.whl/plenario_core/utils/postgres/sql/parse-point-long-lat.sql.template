CREATE OR REPLACE FUNCTION
  parsepointlonglat(long NUMERIC, lat NUMERIC)
RETURNS GEOMETRY(POINT, {{ srid }}) AS $$

  SELECT ST_PointFromText(
    'POINT(' || long || ' ' || lat || ')',
    {{ srid }}
  )

$$
LANGUAGE 'sql' IMMUTABLE;
