A python library implementing the Cube26 cipher

Supports characters A-Z only!

Usage:

from pycube import Cube  
Cube("KEY").encrypt("TEXT")
or  
Cube("KEY").decrypt("YTFY")

# With nonce support:
Cube("KEY", "NONCE").encrypt("TEST")
