============
Installation
============

At the command line::

    $ pip install taxydromikilib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv taxydromikilib
    $ pip install taxydromikilib
