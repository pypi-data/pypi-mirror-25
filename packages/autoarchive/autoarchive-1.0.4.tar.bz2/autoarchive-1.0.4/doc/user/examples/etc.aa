# AutoArchive's archive specification file for the /etc directory

# This is an example file.  You might want to copy it to the
# "/root/.config/aa/archive_specs" directory and issue command "aa -i etc" as
# root in order to create (incremental) backup of configuration files from the
# /etc directory.

[Content]
path = /
include-files = etc
exclude-files =
