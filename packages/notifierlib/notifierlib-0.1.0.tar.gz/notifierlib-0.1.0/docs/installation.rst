============
Installation
============

At the command line::

    $ pip install notifierlib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv notifierlib
    $ pip install notifierlib
