#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['roro']

package_data = \
{'': ['*']}

install_requires = \
['firefly-python>=0.1.4', 'click>=6.7', 'tabulate>=0.7.7', 'PyYAML>=3.12']

entry_points = \
{'console_scripts': ['roro = roro.cli:cli', 'roro-dev = roro.cli:main_dev']}

setup(name='roro',
      version='0.1.2',
      description='roro - commandline tool for accessing all the sevices in RorodataPlatform.',
      author='rorodata',
      author_email='rorodata.team@gmail.com',
      url='https://github.com/rorodata/roro-client',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
