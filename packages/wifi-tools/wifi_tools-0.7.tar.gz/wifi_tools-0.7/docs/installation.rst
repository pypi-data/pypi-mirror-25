.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install wifi_tools

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wifi_tools
    $ pip install wifi_tools
